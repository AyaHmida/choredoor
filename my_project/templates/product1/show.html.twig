{% extends 'base.html.twig' %}




{% block section %}
    <h1>Product1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product1.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product1.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product1_index') }}">back to list</a>

    <a href="{{ path('app_product1_edit', {'id': product1.id}) }}">edit</a>

    {{ include('product1/_delete_form.html.twig') }}
{% endblock %}
