{% extends 'base.html.twig' %}

{% block section %}
<div class="container">
<br>
    <h1>Edit Parfums</h1>

    {{ include('parfums12/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_parfums12_index') }}">back to list</a></div>

    {{ include('parfums12/_delete_form.html.twig') }}
{% endblock %}
