{% extends 'base.html.twig' %}

{% block section %}
<div class="container">
<br><br>
<br><br><br><br>
    <h1>Parfums</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parfum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ parfum.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ parfum.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ parfum.photo }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ parfum.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parfums12_index') }}">back to list</a>

    <a href="{{ path('app_parfums12_edit', {'id': parfum.id}) }}">edit</a></div>

    {{ include('parfums12/_delete_form.html.twig') }}
{% endblock %}
